# ============================================
# AI Sign Language Translator Pro
# Requirements — Python 3.10+
# ============================================

# ── Core Streamlit & WebRTC ──────────────────
streamlit>=1.35.0
streamlit-webrtc>=0.47.0
av>=12.0.0

# ── Computer Vision & Landmarks ─────────────
opencv-python-headless>=4.9.0.80
mediapipe>=0.10.11

# ── Deep Learning ───────────────────────────
tensorflow>=2.16.0

# ── Data & Math ─────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0

# ── Export Libraries ────────────────────────
fpdf2>=2.7.9
python-docx>=1.1.0

# ── NLP (Optional — T5 neural fallback) ─────
# Install these for the optional T5 grammar-correction engine.
# The app works without them using the built-in ASL rule-based engine.
# transformers>=4.40.0
# torch>=2.2.0

# ── Networking & Speech (translation & audio TTS APIs) ──
requests>=2.31.0
gTTS>=2.5.0

# ── Advanced Web UI Server (FastAPI + WebSockets) ──
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
jinja2>=3.1.3

